---
---

<section id="mission" class="mission">
	<div class="mission-content">
		<h2>Our <span class="gradient-text">Mission</span></h2>
		<p class="mission-statement">
			KINETIO’s mission is to elevate human performance through intelligent motion. We combine AI, biomechanics, and wearable technology to help people move smarter, safer, and more efficiently.
		</p>
		<div class="taglines">
			<blockquote>
				"Performance Through Intelligence."
			</blockquote>
		</div>
	</div>
</section>

<style>
	.mission {
		padding: 6em 0;
		text-align: center;
		background: linear-gradient(180deg, transparent 0%, rgba(0, 229, 255, 0.03) 50%, transparent 100%);
		overflow: visible;
		width: 100vw;
		position: relative;
		left: 50%;
		right: 50%;
		margin-left: -50vw;
		margin-right: -50vw;
	}
	.mission-content {
		max-width: 800px;
		margin: 0 auto;
		padding: 0 1em;
	}
	h2 {
		font-size: 3em;
		margin-bottom: 1em;
	}
	.gradient-text {
		background: var(--gradient-accent);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}
	.mission-statement {
		font-size: 1.5em;
		line-height: 1.6;
		color: var(--text-main);
		margin-bottom: 2em;
	}
	blockquote {
		font-size: 2em;
		font-weight: 700;
		color: var(--accent);
		border-left: none; /* Override global default */
		padding: 0;
		font-style: italic;
	}
</style>
